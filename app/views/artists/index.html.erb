<ul>
  <% @artists.each do |artist| %>
  <li>

    <%=  link_to artist.name, artist_path(artist) %>
    (<%= artist.songs.count %> <%= "song".pluralize(artist.songs.count) %>)

  </li>
  <% end %>
</ul>
